# PyAudio wheels are unavailable on newer Windows/Python combos (e.g., 3.14),
# so only install it on versions where binary wheels are commonly available.
PyAudio==0.2.14; python_version < "3.13"
SpeechRecognition>=3.10.0
pyttsx3>=2.90
pyautogui>=0.9.54
Pillow>=10.0.0
pytesseract>=0.3.10
